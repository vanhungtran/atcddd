% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{resolve_batch}
\alias{resolve_batch}
\title{Resolve multiple drug names at once}
\usage{
resolve_batch(queries, source = c("hybrid", "local", "live"), ...)
}
\arguments{
\item{queries}{Character vector of drug names to resolve.}

\item{source}{Character; \code{"hybrid"}, \code{"local"}, or \code{"live"}.
Passed to \code{\link[=resolve_atc]{resolve_atc()}}. Default is \code{"hybrid"}.}

\item{...}{Additional arguments passed to \code{\link[=resolve_atc]{resolve_atc()}}.}
}
\value{
A tibble stacking the results from each query, with the same
columns as \code{\link[=resolve_atc]{resolve_atc()}}.
}
\description{
Vectorised version of \code{\link[=resolve_atc]{resolve_atc()}} — resolves a character vector of
drug names to their ATC codes. Runs sequentially with rate limiting to
be respectful of the WHO server when live fallback is used.
}
\examples{
\donttest{
meds <- c("aspirin", "paracetamol", "ibuprofen", "metformin")
resolve_batch(meds)

# Offline only
resolve_batch(meds, source = "local")
}

}
\seealso{
\code{\link[=resolve_atc]{resolve_atc()}} for single-name resolution.
}
