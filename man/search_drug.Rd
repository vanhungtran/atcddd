% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_drug}
\alias{search_drug}
\title{Search for drugs by name in the ATC database}
\usage{
search_drug(query, data = NULL, max_results = 20L)
}
\arguments{
\item{query}{Character scalar; the drug name or partial name to search for.
Not case-sensitive. Examples: \code{"aspirin"}, \code{"paracetamol"}, \code{"statin"}.}

\item{data}{Optional data frame to search (from \code{\link[=atc_load_db]{atc_load_db()}} or a
custom source). If \code{NULL}, the bundled database is loaded automatically.}

\item{max_results}{Integer; maximum number of results to return.
Default is 20.}
}
\value{
A tibble of matching ATC codes and names, sorted by match quality
(best matches first). Returns a 0-row tibble if no matches are found.
}
\description{
Searches the bundled WHO ATC database for drugs matching a query string.
Performs a multi-stage search: exact match → prefix match → substring
match → word-boundary match. Results are ranked by match quality.

This function works \strong{fully offline} against the bundled data — no
internet connection is needed once the database is loaded.
}
\section{Match Types}{

Results include a \code{match_type} column indicating how each result was found:
\itemize{
\item \code{exact} — query exactly equals the drug name
\item \code{starts_with} — drug name starts with the query
\item \code{contains} — drug name contains the query as a substring
\item \code{word_match} — query matches a whole word within the drug name
}
}

\examples{
\donttest{
search_drug("aspirin")
search_drug("paracetamol")
search_drug("statin", max_results = 10)
search_drug("hydrocortisone")
}

}
\seealso{
\code{\link[=fuzzy_match_drug]{fuzzy_match_drug()}} for typo-tolerant matching,
\code{\link[=resolve_atc]{resolve_atc()}} for hybrid local/live resolution.
}
