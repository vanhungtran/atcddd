% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crawl.R
\name{atc_crawl}
\alias{atc_crawl}
\title{Crawl the WHO ATC/DDD Index}
\usage{
atc_crawl(
  roots = atc_roots_default(),
  rate = 0.5,
  progress = interactive(),
  max_codes = Inf,
  quiet = FALSE
)
}
\arguments{
\item{roots}{Character vector of root ATC codes to start from. Must be
uppercase codes. Default: \code{atc_roots_default()}.}

\item{rate}{Numeric; minimum seconds between HTTP requests (default: 0.5).}

\item{progress}{Logical; show a progress bar (default: \code{interactive()}).}

\item{max_codes}{Integer; limit on number of codes to visit (default: \code{Inf}).}

\item{quiet}{Logical; reduce informational messages (default: FALSE).}
}
\value{
A list with \code{codes} and \code{ddd} tibbles.
}
\description{
Iteratively traverses ATC codes starting from one or more root codes,
respecting rate limits and using the HTTP layer's caching. Returns two
tidy tables: \code{codes} (unique ATC codes with names) and \code{ddd}
(dose definitions and related fields).
}
\examples{
\dontrun{
res <- atc_crawl(roots = "D", rate = 0.8, max_codes = 50)
head(res$codes)
head(res$ddd)
}

}
\seealso{
\code{\link[=atc_write_csv]{atc_write_csv()}}, \code{\link[=atc_roots_default]{atc_roots_default()}}
}
