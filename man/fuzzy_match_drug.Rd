% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{fuzzy_match_drug}
\alias{fuzzy_match_drug}
\title{Fuzzy-match a drug name to ATC codes}
\usage{
fuzzy_match_drug(query, data = NULL, max_distance = 3L, max_results = 10L)
}
\arguments{
\item{query}{Character scalar; the drug name to match.}

\item{data}{Optional data frame to search. If \code{NULL}, loads the bundled database.}

\item{max_distance}{Integer; maximum allowed Levenshtein distance.
Lower values are stricter. Default is 3.}

\item{max_results}{Integer; maximum results to return. Default is 10.}
}
\value{
A tibble of the closest-matching ATC codes, sorted by edit
distance (closest first). Includes a \code{distance} column with the
Levenshtein distance to each match.
}
\description{
Finds ATC codes for drug names that may be misspelled, abbreviated, or
use non-standard formatting. Uses Levenshtein (edit) distance via
\code{utils::adist()} to find close matches in the bundled WHO database.

This is useful when:
\itemize{
\item Users type drug names from memory (typos)
\item Clinical notes use non-standard spellings
\item Brand names or abbreviations differ from WHO naming
}
}
\examples{
\donttest{
# Common misspellings
fuzzy_match_drug("acetominophen")    # should find "paracetamol"
fuzzy_match_drug("asprin")           # should find "aspirin"
fuzzy_match_drug("metmorphin")       # should find "metformin"

# Stricter matching
fuzzy_match_drug("asprin", max_distance = 1)
}

}
\seealso{
\code{\link[=search_drug]{search_drug()}} for exact/substring matching,
\code{\link[=resolve_atc]{resolve_atc()}} for hybrid resolution.
}
