% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{resolve_atc}
\alias{resolve_atc}
\title{Resolve a drug name to its ATC code with hybrid local/live lookup}
\usage{
resolve_atc(
  query,
  source = c("hybrid", "local", "live", "rxnorm"),
  data = NULL,
  fuzzy = TRUE,
  rate_limit = 0.5
)
}
\arguments{
\item{query}{Character scalar; the drug name to resolve.
Case-insensitive. Examples: \code{"aspirin"}, \code{"atorvastatin"}.}

\item{source}{Character; resolution strategy:
\itemize{
\item \code{"local"} — cached database only (offline, fast)
\item \code{"live"} — WHO website only (always up-to-date, requires internet)
\item \code{"hybrid"} — try local first, fall back to live (default)
}}

\item{data}{Optional data frame for local search. If \code{NULL}, auto-loads
the cached database.}

\item{fuzzy}{Logical; if \code{TRUE} and the exact lookup fails, fall back
to fuzzy matching before trying live resolution. Default is \code{TRUE}.}

\item{rate_limit}{Numeric; delay between live WHO requests (seconds).
Only used when \code{source} is \code{"live"} or \code{"hybrid"}. Default is 0.5.}
}
\value{
A tibble with columns:
\itemize{
\item \code{query} — the original query
\item \code{atc_code} — the resolved ATC code
\item \code{atc_name} — the drug name from the database
\item \code{source} — \code{"local"} or \code{"live"} indicating where the match came from
\item \code{match_type} — how the match was found (\code{"exact"}, \code{"fuzzy"}, \code{"live"})
}
Returns a 0-row tibble if the drug cannot be resolved.
}
\description{
The primary drug-name resolution function. Tries to find the ATC code
for a given drug name by searching the cached local database first,
then optionally falling back to a live WHO crawl if the drug is not
found locally.

This is the function you reach for when you have a drug name and need
its ATC code — the #1 workflow in pharmacoepidemiology.
}
\section{Resolution Order (hybrid mode)}{

\enumerate{
\item Exact name match in local database
\item Fuzzy match in local database (if \code{fuzzy = TRUE})
\item Live WHO crawl as last resort
}
}

\examples{
\donttest{
# Offline — fast, no internet needed
resolve_atc("aspirin", source = "local")

# Live — always current
resolve_atc("aspirin", source = "live")

# Hybrid (default) — local first, live fallback
resolve_atc("atorvastatin")

# Batch resolution
meds <- c("aspirin", "metformin", "atorvastatin")
resolve_batch(meds)
}

}
\seealso{
\code{\link[=resolve_batch]{resolve_batch()}} for vectorised resolution,
\code{\link[=search_drug]{search_drug()}} for exploratory search.
}
